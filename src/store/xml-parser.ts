import type { ContentNode } from '../types.js';

/**
 * Convert USLM XML string to a JSON ContentNode tree.
 * Uses fast-xml-parser under the hood.
 *
 * TODO: Implement XML → JSON conversion
 */
export function uslmXmlToJson(_xml: string): ContentNode[] {
  // Placeholder — will use fast-xml-parser to parse USLM XML
  // and map elements to ContentNode structure
  return [];
}
